'use strict';

(function(app){
  app.service('User', function($http, $log, $window, $q, $rootScope, $location, ConfigProvider) {
    var user = {};

    return user;
  });
})(window.bgChatServicesApp);
